-- Executado automaticamente só na PRIMEIRA vez que o Postgres sobe (volume vazio).
-- O banco "evolution" já é criado pela variável POSTGRES_DB.
-- Aqui criamos o banco do nosso sistema.
CREATE DATABASE central;
